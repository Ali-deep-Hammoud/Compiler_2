<!DOCTYPE html>
<html>

<head>
    <title>>Product App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #4b4b4b;
        }

        nav {
            background: #131313;
            padding: 10px;
        }

        nav a {
            color: #fff;
            margin-right: 15px;
            text-decoration: none;
            font-weight: bold;
        }

        .container {
            max-width: 900px;
            margin: 20px auto;
        }

        .product-grid {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr 1fr;
            /* 4 equal columns */
            gap: 20px;
        }


        .product-card {
            padding: 15px;
            background: #c0c0c0;
            border-radius: 8px;
            position: relative;
            text-align: center;
            padding-bottom: 60px;

        }

        .product-card img {
            width: 100%;
            border-radius: 5px;
        }

        .detail-img {
            width: 300px;
            border-radius: 10px;
            display: block;
            margin-bottom: 20px;
        }

        form label {
            display: block;
            margin-top: 10px;
        }

        form input,
        form textarea {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
        }

        .btn {
            display: inline-block;


            margin-top: 15px;
            padding: 10px 15px;
            background: hsl(0, 100%, 22%);
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }

        #button {
            position: absolute;
            /* Add this */
            bottom: 15px;
            /* Stick to bottom */
            left: 50%;
            transform: translateX(-50%);
            width: calc(100% - 100px);
            /* Optional: make width match padding */

        }
    </style>
</head>

<body>

    <nav>
        <a href="{{ url_for('index') }}">Home</a>
        <a href="{{ url_for('add_product') }}">Add Product</a>
    </nav>

    <div class="container">
        {% block content %}{% endblock %}
    </div>

</body>

</html>
